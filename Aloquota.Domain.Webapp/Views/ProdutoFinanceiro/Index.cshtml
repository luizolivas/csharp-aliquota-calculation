@model IEnumerable<Aliquota.Domain.Webapp.Models.ProdutoFinanceiroViewModel>

<h2>Produtos Financeiros</h2>
<hr />

<div class="row">
    <div class="col-md-12">
        <div>
            <div class="pull-left">
                <a asp-action="NovoProdutoFinanceiro" class="btn btn-primary">
                    <span title="Create New" class="fas fa-plus"></span> Adicionar
                </a>
            </div>
        </div>
    </div>
</div>

<br />

<div class="panel panel-default">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Valor)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DataAplicacao)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.NomeCliente)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Valor)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DataAplicacao)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NomeCliente)
                    </td>
                    <td>
                        <a asp-action="AtualizaProduto" asp-route-id="@item.Id" title="Edita" class="btn btn-warning">
                            <span class="fas fa-edit"></span> Atualizar
                        </a>
                        <a asp-action="DeletaProduto" asp-route-id="@item.Id" title="Deleta" class="btn btn-danger">
                            <span class="fas fa-trash-alt"></span> Excluir
                        </a>
                        <a asp-action="AplicaProduto" asp-route-id="@item.Id" title="Aplica" class="btn btn-primary">
                            <span class="fas fa-trash-alt"></span> Aplicar
                        </a>
                        <a asp-action="DeletaProduto" asp-route-id="@item.Id" title="Resgata" class="btn btn-outline-primary">
                            <span class="fas fa-trash-alt"></span> Resgatar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
